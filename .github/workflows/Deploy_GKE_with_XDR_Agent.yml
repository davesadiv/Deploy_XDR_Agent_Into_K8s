name: XDR GKE Deployment (Helm)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  REGION: "us-central1"
  OS_TYPE: "linux"
  PACKAGE_NAME: "cortex-xdr-chart.tgz" # Updated to reflect Helm archive

jobs:
  healthcheck:
    runs-on: ubuntu-latest
    environment: Cortex
    steps:
      - name: XDR API Healthcheck
        env:
          XDR_URL: ${{ secrets.XDR_URL }}
          AUTH_ID: ${{ secrets.AUTH_ID }}
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
        run: |
          health_response=$(curl -s "$XDR_URL/public_api/v1/healthcheck" \
            -H "Accept: application/json" \
            -H "x-xdr-auth-id: $AUTH_ID" \
            -H "Authorization: $AUTH_TOKEN")
          [ "$(echo "$health_response" | jq -r '.status')" != "available" ] && exit 1
          echo "API status verified."

  distribution:
    needs: healthcheck
    runs-on: ubuntu-latest
    environment: Cortex
    steps:
      - name: Generate XDR Helm Chart
        env:
          XDR_URL: ${{ secrets.XDR_URL }}
          AUTH_ID: ${{ secrets.AUTH_ID }}
          AUTH_TOKEN: ${{ secrets.AUTH_TOKEN }}
        run: |
          set -e
          # 1. Get Latest Version
          agent_versions=$(curl -s -X POST "$XDR_URL/public_api/v1/distributions/get_versions" \
            -H "Content-Type: application/json" \
            -H "x-xdr-auth-id: $AUTH_ID" \
            -H "Authorization: $AUTH_TOKEN" -d '{}')
          
          latest_ver=$(echo "$agent_versions" | jq -r ".reply.\"$OS_TYPE\"[]" | tail -n 1)
          
          # 2. Create Distribution (Switched to package_type: helm)
          PAYLOAD=$(jq -n --arg pipe "$GITHUB_RUN_ID" --arg ver "$latest_ver" \
            '{request_data: {name: ("GH-" + $pipe), package_type: "helm", agent_version: $ver, deployment_platform: "standard", default_namespace: "cortex-xdr"}}')
          
          dist_res=$(curl -s -X POST "$XDR_URL/public_api/v1/distributions/create" \
            -H "Content-Type: application/json" \
            -H "x-xdr-auth-id: $AUTH_ID" \
            -H "Authorization: $AUTH_TOKEN" -d "$PAYLOAD")
          
          dist_id=$(echo "$dist_res" | jq -r '.reply.distribution_id // empty')
          
          echo "Waiting 120s for Helm chart generation..."
          sleep 120
          
          # 3. Get Dist URL (Switched to package_type: helm)
          url_response=$(curl -s -X POST "$XDR_URL/public_api/v1/distributions/get_dist_url" \
            -H "Content-Type: application/json" \
            -H "x-xdr-auth-id: $AUTH_ID" \
            -H "Authorization: $AUTH_TOKEN" \
            -d "{\"request_data\": {\"distribution_id\": \"$dist_id\", \"package_type\": \"helm\"}}")
          
          dist_url=$(echo "$url_response" | jq -r '.reply.distribution_url // empty')
          
          # Masking the signed URL for security
          echo "::add-mask::$dist_url"
          
          curl --fail -L -X POST "$dist_url" -H "x-xdr-auth-id: $AUTH_ID" -H "Authorization: $AUTH_TOKEN" -o "$PACKAGE_NAME"

      - name: Upload Helm Chart
        uses: actions/upload-artifact@v4
        with:
          name: xdr-helm-package
          path: ${{ env.PACKAGE_NAME }}
          retention-days: 1

  deploy:
    needs: distribution
    runs-on: ubuntu-latest
    environment: Cortex
    steps:
      - name: Download Helm Chart
        uses: actions/download-artifact@v4
        with:
          name: xdr-helm-package

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          install_components: 'gke-gcloud-auth-plugin'

      - name: Get GKE Credentials
        uses: google-github-actions/get-gke-credentials@v2
        with:
          cluster_name: ${{ vars.CLUSTER_NAME }}
          location: ${{ env.REGION }}
          project_id: ${{ vars.GCP_PROJECT_NAME }}

      - name: Deploy via Helm
        run: |
          # Create namespace if it doesn't exist
          kubectl create namespace cortex-xdr --dry-run=client -o yaml | kubectl apply -f -
          
          # Helm upgrade/install handles the DaemonSet logic
          helm upgrade --install cortex-xdr-agent ./${{ env.PACKAGE_NAME }} \
            --namespace cortex-xdr \
            --wait \
            --timeout 5m
          
          echo "Checking Pod Status..."
          kubectl get pods -n cortex-xdr
          
          READY_COUNT=$(kubectl get pods -n cortex-xdr --no-headers | grep "1/1" | wc -l)
          
          if [ "$READY_COUNT" -gt 0 ]; then
            echo "Success: $READY_COUNT XDR agent(s) are Ready and Running."
          else
            echo "Error: Agents failed to reach Ready status."
            kubectl get events -n cortex-xdr --sort-by='.lastTimestamp' | tail -n 5
            exit 1
          fi
